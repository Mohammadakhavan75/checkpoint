# AI Task Workspaces

Each task gets its own directory containing:

- `plan.md`
- `session-log.md`
- optional notes, hypotheses, command logs, or investigation artifacts

Create a workspace with:

```bash
scripts/ai_new_task.sh TASK-0001 short-name
```
