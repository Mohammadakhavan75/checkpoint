import * as React from 'react';

/**
 * Loading — from @checkpoint/ds@1.0.0.
 */
export interface LoadingProps {
  [key: string]: unknown;
}

export declare const Loading: React.ComponentType<LoadingProps>;
