# Runtime dependencies for the Checkpoint API
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
bcrypt>=4.0
PyJWT>=2.8
python-multipart>=0.0.9
email-validator>=2.1
google-auth[requests]>=2.30
# cert-fetch HTTP caching for Google sign-in (services/google_auth.py)
cachecontrol>=0.14
# Fernet encryption for OAuth tokens at rest (services/crypto.py, calendar connect)
cryptography>=43
# Calendar connect makes plain REST calls to Google with requests (pulled in by
# google-auth[requests]); pinned explicitly since services/calendar_sync uses it.
requests>=2.31
