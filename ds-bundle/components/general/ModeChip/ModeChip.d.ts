import * as React from 'react';

/**
 * ModeChip — from @checkpoint/ds@1.0.0.
 */
export interface ModeChipProps {
  /** Session mode: "Do", "Scout", "Plan" — or any custom label */
  mode: string;
}

export declare const ModeChip: React.ComponentType<ModeChipProps>;
