# Dev server only (vite build runs in production mode and never reads this).
# Same-origin /api, proxied to the local API by server.proxy in vite.config.ts,
# so the dev server works from any port without touching the API's CORS list.
VITE_API_BASE=/api
