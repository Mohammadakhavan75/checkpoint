ProgressBar from @checkpoint/ds. Use via `window.CheckpointDS.ProgressBar` (bundle loaded from the root `_ds_bundle.js`).

Slim inline progress bar. Wraps `.prog` / `.bar`.

## Props

```ts
interface ProgressBarProps {
  /** Completion percentage, 0–100 */
  value: number;
}
```
